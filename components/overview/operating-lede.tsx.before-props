import { operatingLede, opsData } from "@/lib/ops-data"

export function OperatingLede() {
  return (
    <section className="operating-lede" aria-labelledby="operating-lede-title">
      <div>
        <p className="story-kicker">WHERE WE ARE NOW Â· {opsData.meta.block}</p>
        <h2 id="operating-lede-title">{operatingLede()}</h2>
      </div>
      <p className="lede-context">This month Â· day {opsData.meta.day} of {opsData.meta.daysInMonth}<br /><span>Live operations data</span></p>
    </section>
  )
}
