"use client"
import { CartesianGrid, LabelList, ReferenceLine, Scatter, ScatterChart, XAxis, YAxis } from "recharts"
import { ChartContainer, ChartTooltip, ChartTooltipContent } from "@/components/ui/chart"
import { EditorialChartFrame } from "./editorial-chart-frame"
import { CHART_SOURCE_NOTE, EDITORIAL_CHART_COPY, networkAverage, studioArpu } from "@/lib/editorial-charts"
const config = { arpu: { label: "ARPU", color: "var(--chart-1)" } }
export function StudioArpuChart() { return <EditorialChartFrame id="studio-arpu" {...EDITORIAL_CHART_COPY.arpu} source={CHART_SOURCE_NOTE} table={<table><caption>Theatre ARPU ranking</caption><thead><tr><th>Theatre</th><th>ARPU</th></tr></thead><tbody>{studioArpu.map(d => <tr key={d.theatre}><th>{d.theatre}</th><td>₹{d.value.toLocaleString("en-IN")}</td></tr>)}<tr><th>{networkAverage.label}</th><td>₹{networkAverage.value.toLocaleString("en-IN")}</td></tr></tbody></table>}><ChartContainer config={config} className="editorial-chart-canvas"><ScatterChart accessibilityLayer margin={{ left: 18, right: 58, top: 14, bottom: 8 }}><CartesianGrid horizontal={false} /><XAxis type="number" dataKey="value" domain={[1600, 1850]} tickFormatter={v => `₹${v}`} tickLine={false} axisLine={false} /><YAxis type="category" dataKey="theatre" width={128} tickLine={false} axisLine={false} /><ReferenceLine x={networkAverage.value} stroke="var(--interactive-blue-soft)" strokeDasharray="4 4" label={{ value: "Network ₹1,742", position: "insideTopRight" }} /><ChartTooltip content={<ChartTooltipContent />} /><Scatter data={studioArpu} fill="var(--color-arpu)"><LabelList dataKey="value" position="right" formatter={v => `₹${Number(v).toLocaleString("en-IN")}`} /></Scatter></ScatterChart></ChartContainer></EditorialChartFrame> }
